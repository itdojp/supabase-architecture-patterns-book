# 📚 クイックスタート リファレンス

このページは、既にセットアップが完了している方向けの簡単なリファレンスです。

**初回セットアップの方は**: [📖 完全セットアップガイド](setup-guide.md) をご覧ください。

## 5分で新しい書籍プロジェクトを開始

### 前提条件
- [ ] Node.js 18+ がインストール済み
- [ ] GitHubアカウントを持っている

### ステップ1: テンプレートを使用

1. [本リポジトリ](https://github.com/itdojp/book-publishing-template) で「Use this template」をクリック
2. プライベートリポジトリ名を入力（例: `my-book-private`）
3. リポジトリを作成

### ステップ2: ローカルセットアップ

```bash
# クローンと移動
git clone https://github.com/yourusername/my-book-private.git
cd my-book-private

# 依存関係のインストールと初期化
npm install
npm run init
```

質問に答えて設定を完了：

- 書籍タイトル
- 著者名
- GitHubユーザー名

### ステップ3: GitHub設定

1. **パブリックリポジトリを作成** (空のリポジトリ)
2. **Personal Access Token** を [生成](https://github.com/settings/tokens/new) (`repo` スコープ)
3. プライベートリポジトリの **Settings → Secrets** に `DEPLOY_TOKEN` を追加

### ステップ4: デプロイ

```bash
# ビルドとデプロイ
npm run build
npm run deploy:full
```

### ステップ5: GitHub Pages設定

パブリックリポジトリで:
1. **Settings → Pages**
2. **Source**: Deploy from a branch
3. **Branch**: gh-pages / (root)
4. **Save**

数分後に `https://yourusername.github.io/your-book-public/` でアクセス可能！

## よく使うコマンド

### 基本コマンド

| コマンド | 説明 |
|----------|------|
| `npm run build` | フルビルド |
| `npm run build:incremental` | インクリメンタルビルド（高速） |
| `npm run preview` | ローカルプレビュー (http://localhost:8080) |
| `npm run deploy` | 増分デプロイ |
| `npm run deploy:full` | フルデプロイ |
| `npm run clean` | ビルドキャッシュをクリア |

数分後に `https://yourusername.github.io/your-book-public/` でアクセス可能！

### 開発ワークフロー

```bash
# 1. 新しい章を追加
mkdir -p src/chapters/chapter02
echo "# 第2章 タイトル" > src/chapters/chapter02/index.md

# 2. ローカルで確認
npm run build
npm run preview

# 3. コミット&デプロイ
git add .
git commit -m "Add chapter 2"
git push origin main  # 自動デプロイされる
```

## ディレクトリ構造

```
my-book-private/
├── src/
│   ├── introduction/      # はじめに
│   ├── chapters/         # 本章
│   │   ├── chapter01/
│   │   └── chapter02/
│   ├── appendices/       # 付録
│   └── afterword/        # あとがき
├── assets/images/        # 画像ファイル
├── scripts/             # ビルドスクリプト
└── public/              # ビルド出力（自動生成）
```

## トラブルシューティング

### よくある問題

| 問題 | 解決方法 |
|------|----------|
| ビルドエラー | `npm run clean && npm install && npm run build` |
| デプロイ失敗 | DEPLOY_TOKEN の設定・有効期限を確認 |
| 404エラー | GitHub Pages設定とgh-pagesブランチを確認 |
| 数式が表示されない | MathJax設定を確認 |

### 詳細情報

- 🔧 [完全セットアップガイド](setup-guide.md)
- 📚 [テンプレート構造](template-structure.md)
- 🚀 [デプロイメントガイド](deployment-guide.md)

---

💡 **Tip**: より詳細な手順や高度な設定については、[完全セットアップガイド](setup-guide.md)をご覧ください。
