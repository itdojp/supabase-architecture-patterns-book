# 🔧 トラブルシューティングガイド

このガイドでは、Book Publishing Templateを使用する際によく発生する問題とその解決方法を説明します。

## 目次

1. [セットアップ関連](#セットアップ関連)
2. [ビルド関連](#ビルド関連)
3. [デプロイ関連](#デプロイ関連)
4. [GitHub Pages関連](#github-pages関連)
5. [コンテンツ関連](#コンテンツ関連)
6. [パフォーマンス関連](#パフォーマンス関連)
7. [エラーメッセージ一覧](#エラーメッセージ一覧)

## セットアップ関連

### npm installが失敗する

**症状**: `npm install`実行時にエラーが発生する

**原因と解決方法**:

1. **Node.jsバージョンが古い**
   ```bash
   # Node.jsバージョンを確認
   node --version
   
   # 16以上でない場合は更新
   # nvmを使用している場合
   nvm install 18
   nvm use 18
   ```

2. **npmキャッシュの問題**
   ```bash
   # npmキャッシュをクリア
   npm cache clean --force
   
   # 再度インストール
   npm install
   ```

3. **権限の問題（Linux/macOS）**
   ```bash
   # グローバルインストールではないので、sudoは不要
   # プロジェクトディレクトリの権限を確認
   ls -la
   
   # 必要に応じて権限を修正
   chmod -R 755 .
   ```

### npm run initが動作しない

**症状**: 初期化スクリプトが見つからない、または失敗する

**解決方法**:

```bash
# スクリプトの存在を確認
ls scripts/init-template.js

# ファイルが存在しない場合
# テンプレートから再度クローン、または手動で作成

# package.jsonを確認
cat package.json | grep init

# スクリプトが登録されていない場合
npm pkg set scripts.init="node scripts/init-template.js"
```

## ビルド関連

### ビルドが遅い

**症状**: `npm run build`の実行に時間がかかる

**解決方法**:

1. **インクリメンタルビルドを使用**
   ```bash
   # 変更されたファイルのみビルド
   npm run build:incremental
   ```

2. **不要なファイルを除外**
   ```json
   // book-config.json
   {
     "excludePatterns": [
       "*.tmp",
       "*.backup",
       "old/**/*"
     ]
   }
   ```

### ビルドエラー: Cannot find module

**症状**: モジュールが見つからないエラー

**解決方法**:

```bash
# node_modulesを削除して再インストール
rm -rf node_modules package-lock.json
npm install

# 特定のモジュールが不足している場合
npm install [モジュール名]
```

### 日本語が文字化けする

**症状**: ビルド後のファイルで日本語が正しく表示されない

**原因**: ファイルのエンコーディング問題

**解決方法**:

1. **ソースファイルのエンコーディングを確認**
   ```bash
   # ファイルのエンコーディングを確認
   file -i src/chapters/chapter01/index.md
   
   # UTF-8に変換
   iconv -f SHIFT-JIS -t UTF-8 input.md > output.md
   ```

2. **エディタの設定を確認**
   - VSCode: 設定で`"files.encoding": "utf8"`
   - その他のエディタ: UTF-8として保存

## デプロイ関連

### デプロイ時の認証エラー

**症状**: `npm run deploy`で認証に失敗する

**エラーメッセージ例**:
```
remote: Permission to username/repo.git denied to username.
fatal: unable to access 'https://github.com/username/repo.git/': The requested URL returned error: 403
```

**解決方法**:

1. **トークンの権限を確認**
   - GitHub → Settings → Developer settings → Personal access tokens
   - `repo`スコープが有効になっているか確認
   - 期限切れでないか確認

2. **環境変数を確認**
   ```bash
   # GitHub Actions使用時
   # Settings → Secrets → DEPLOY_TOKEN が設定されているか確認
   
   # ローカル実行時
   echo $GITHUB_TOKEN
   echo $PUBLIC_REPO_URL
   ```

3. **新しいトークンを生成**
   ```bash
   # 新しいトークンを生成して環境変数に設定
   export GITHUB_TOKEN="ghp_新しいトークン"
   export PUBLIC_REPO_URL="https://github.com/username/my-book-public.git"
   ```

### デプロイ先のリポジトリが見つからない

**症状**: 公開リポジトリへのプッシュが失敗する

**解決方法**:

1. **リポジトリURLの確認**
   ```bash
   # deploy.shの設定を確認
   grep REPO_URL scripts/deploy.sh
   
   # 環境変数を設定
   export PUBLIC_REPO_URL="https://github.com/YOUR_USERNAME/YOUR_PUBLIC_REPO.git"
   ```

2. **リポジトリの存在確認**
   - GitHubで公開リポジトリが作成されているか確認
   - リポジトリ名のタイポがないか確認

### gh-pagesブランチが作成されない

**症状**: 初回デプロイでブランチ作成に失敗

**解決方法**:

```bash
# 手動でgh-pagesブランチを作成
cd /tmp
git clone https://github.com/USERNAME/PUBLIC_REPO.git
cd PUBLIC_REPO
git checkout --orphan gh-pages
git rm -rf .
echo "# GitHub Pages" > README.md
git add README.md
git commit -m "Initial gh-pages branch"
git push origin gh-pages

# 再度デプロイを実行
cd /path/to/private-repo
npm run deploy
```

## GitHub Pages関連

### サイトが404エラーになる

**症状**: GitHub Pagesのアドレスにアクセスすると404エラー

**チェックリスト**:

1. **GitHub Pages設定の確認**
   - Settings → Pages
   - Source: Deploy from a branch
   - Branch: gh-pages / (root)

2. **デプロイの成功確認**
   - Actions タブでワークフローの状態を確認
   - gh-pagesブランチに内容があるか確認

3. **URLの確認**
   ```
   # 正しいURL形式
   https://USERNAME.github.io/REPOSITORY_NAME/
   
   # 末尾のスラッシュに注意
   ```

4. **_config.ymlのbaseurl確認**
   ```yaml
   # _config.yml
   baseurl: "/my-book-public"  # リポジトリ名と一致させる
   ```

### CSSが読み込まれない

**症状**: ページは表示されるがスタイルが適用されない

**解決方法**:

1. **baseurlの設定確認**
   ```yaml
   # _config.yml
   baseurl: "/リポジトリ名"
   url: "https://ユーザー名.github.io"
   ```

2. **_layouts/default.htmlの確認**
   ```html
   <!-- 正しいパス指定 -->
   <link rel="stylesheet" href="{{ '/assets/css/style.css' | relative_url }}">
   ```

### 更新が反映されない

**症状**: デプロイしたのに変更が表示されない

**解決方法**:

1. **ブラウザキャッシュをクリア**
   - Ctrl+Shift+R (Windows/Linux)
   - Cmd+Shift+R (Mac)

2. **GitHub Pagesのビルド状態確認**
   - Settings → Pages → 最終ビルド時刻を確認

3. **CDNキャッシュの問題**
   ```bash
   # URLに?v=タイムスタンプを追加
   https://username.github.io/repo/?v=20240616
   ```

## コンテンツ関連

### プライベートコンテンツが公開されてしまう

**症状**: TODO コメントや draft.md が公開版に含まれる

**確認方法**:

```bash
# ビルド結果を確認
grep -r "TODO:" public/
grep -r "PRIVATE:" public/

# draft.mdが含まれていないか確認
find public -name "draft.md"
```

**解決方法**:

1. **book-config.jsonの設定確認**
   ```json
   {
     "excludePatterns": [
       "draft.md",
       "notes.md",
       "private.md"
     ],
     "contentExcludePatterns": [
       "<!-- TODO:",
       "<!-- PRIVATE:",
       "<!-- FIXME:"
     ]
   }
   ```

2. **ビルドキャッシュをクリア**
   ```bash
   npm run clean
   npm run build
   ```

### 目次が生成されない

**症状**: table-of-contents.mdが作成されない、または内容が空

**解決方法**:

1. **設定の確認**
   ```json
   // book-config.json
   {
     "tableOfContents": {
       "enabled": true,
       "outputFile": "table-of-contents.md",
       "maxDepth": 3
     }
   }
   ```

2. **見出しの形式確認**
   ```markdown
   # 正しい見出し形式
   ## サブセクション
   ### サブサブセクション
   
   #間違った形式（スペースなし）
   ##  間違った形式（スペース多すぎ）
   ```

## パフォーマンス関連

### メモリ不足エラー

**症状**: ビルド中にメモリ不足でクラッシュ

**エラーメッセージ**:
```
FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed
```

**解決方法**:

```bash
# Node.jsのメモリ制限を増やす
export NODE_OPTIONS="--max-old-space-size=4096"

# または package.json でスクリプトを修正
"scripts": {
  "build": "node --max-old-space-size=4096 scripts/build.js"
}
```

### ディスク容量不足

**症状**: ビルドや git 操作が失敗

**解決方法**:

```bash
# ディスク使用量を確認
df -h

# 不要なファイルを削除
npm run clean
git gc --aggressive

# node_modulesを削除して再インストール
rm -rf node_modules
npm install
```

## エラーメッセージ一覧

### よくあるエラーメッセージと対処法

| エラーメッセージ | 原因 | 解決方法 |
|-----------------|------|----------|
| `ENOENT: no such file or directory` | ファイルが存在しない | ファイルパスを確認、必要なファイルを作成 |
| `EACCES: permission denied` | 権限不足 | ファイル権限を確認、`chmod`で修正 |
| `Cannot find module 'xxx'` | モジュール未インストール | `npm install xxx` を実行 |
| `SyntaxError: Unexpected token` | JSONやJSの構文エラー | 該当ファイルの構文を確認 |
| `fatal: remote origin already exists` | リモートリポジトリ設定済み | `git remote remove origin` 後に再設定 |
| `error: failed to push some refs` | リモートに新しいコミットがある | `git pull` してから `push` |

## デバッグモード

詳細なログを出力する方法：

```bash
# 環境変数でデバッグモードを有効化
export DEBUG=true

# Gitの詳細ログ
export GIT_TRACE=1
export GIT_CURL_VERBOSE=1

# Node.jsのデバッグ
node --inspect scripts/build.js
```

## それでも解決しない場合

1. **エラーメッセージ全文をコピー**
2. **実行環境の情報を収集**
   ```bash
   node --version
   npm --version
   git --version
   uname -a  # OS情報
   ```
3. **[GitHub Issues](https://github.com/itdojp/book-publishing-template/issues)で質問**
   - エラーメッセージ
   - 実行したコマンド
   - 環境情報
   - 試した解決方法

---

最終更新: 2024年6月